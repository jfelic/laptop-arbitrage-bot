name: Deploy to AWS

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install Dependencies
        run: npm ci
      - name: Compile TypeScript
        run: npx tsc --noEmit

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        run: terraform init
        working-directory: ./terraform

      # Step 1: Create ECR Repo first (so we can push to it)
      - name: Provision ECR
        run: terraform apply -target=aws_ecr_repository.arbitrage_repo -auto-approve
        working-directory: ./terraform
        env:
            TF_VAR_best_buy_api_key: ${{ secrets.BEST_BUY_API_KEY }}
            TF_VAR_apify_api_token: ${{ secrets.APIFY_API_TOKEN }}
            TF_VAR_discord_webhook: ${{ secrets.DISCORD_WEBHOOK }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      # Step 2: Build & Push Image
      - name: Build, tag, and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: arbitrage-bot
          IMAGE_TAG: latest
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      # Step 3: Provision everything else (Lambda, SQS, etc.)
      - name: Provision Infrastructure
        run: terraform apply -auto-approve
        working-directory: ./terraform
        env:
          TF_VAR_best_buy_api_key: ${{ secrets.BEST_BUY_API_KEY }}
          TF_VAR_apify_api_token: ${{ secrets.APIFY_API_TOKEN }}
          TF_VAR_discord_webhook: ${{ secrets.DISCORD_WEBHOOK }}
